# Supabase + Prisma Environment Example
# Copy to .env and fill in actual secrets. Never commit real credentials.

# Supabase project reference (found in dashboard URL)
SUPABASE_PROJECT_ID="your-project-ref"

# Supabase database password (Settings > Database > Connection string)
SUPABASE_DB_PASSWORD="your-db-password"

# Supabase anon/public key (Settings > API)
SUPABASE_ANON_KEY="your-anon-key"
# Supabase service role key (Settings > API) – keep secret server-side only
SUPABASE_SERVICE_ROLE_KEY="your-service-role-key"

# Supabase recommended connection strings (pooler & direct):
# NOTE: Username format is postgres.<project_ref> (dot) not just postgres.
# Region host format uses dot separation (aws-1.eu-west-1), not hyphens.
# Pooler (PgBouncer) port 6543 when pgbouncer=true.
DATABASE_URL="postgresql://postgres.${SUPABASE_PROJECT_ID}:${SUPABASE_DB_PASSWORD}@aws-1.eu-west-1.pooler.supabase.com:6543/postgres?pgbouncer=true&sslmode=require"
# Direct host (no PgBouncer) on 5432 for migrations
DIRECT_URL="postgresql://postgres.${SUPABASE_PROJECT_ID}:${SUPABASE_DB_PASSWORD}@aws-1.eu-west-1.pooler.supabase.com:5432/postgres?sslmode=require"

# Node environment
NODE_ENV="development"

# Better-Auth related (examples, adjust as needed)
AUTH_SECRET="replace-with-random-32-byte"
RESEND_API_KEY="replace-with-resend-key"
STRIPE_SECRET_KEY="replace-with-stripe-key"
STRIPE_WEBHOOK_SECRET="replace-with-webhook-secret"
