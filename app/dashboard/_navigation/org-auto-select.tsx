"use client";

/**
 * Organizations feature removed.
 * Keep a no-op component for backwards compatibility with existing imports.
 */
export const OrgAutoSelect = (_props: { orgs: { slug: string }[] }) => {
  return null;
};
